.dataset-show

  %h2= @dataset.name

  = link_to "Run MOCK on this Dataset", new_run_path(:dataset => @dataset.id)

  %table
    - @dataset.datapoints.each do |datapoint|
      %tr
        - datapoint.datavalues.each do |datavalue|
          %td= "#{datavalue.value} "